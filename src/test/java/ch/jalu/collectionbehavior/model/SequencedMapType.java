package ch.jalu.collectionbehavior.model;

/**
 * Defines whether a Map type implements the SequencedMap interface.
 */
public enum SequencedMapType {

    /** The type implements SequencedMap. */
    IMPLEMENTS,

    /** The type does not implement SequencedMap. */
    DOES_NOT_IMPLEMENT

}
